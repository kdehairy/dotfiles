# Setup New Environment

This needs to be run on a clean home directory. If one or more of the 
tracked files exists, This process will error out.

1- `git clone --bare git@github.com:kdehairy/dotfiles.git $HOME/.dotfiles`
2- `/usr/bin/git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME checkout`
3- `/usr/bin/git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME config --local status.showUntrackedFiles no`
